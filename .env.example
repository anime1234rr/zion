VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Solo para scripts de servidor/CLI. NUNCA la uses en código de src/
# (bypassa RLS por completo). No debe llegar al bundle del renderer.
SUPABASE_SERVICE_ROLE_KEY=
